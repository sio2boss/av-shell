#!/bin/bash

# Import color
source $DSS_ROOT/config/color

# Check for a help flag
if [[ "$1" == "-h" || "$1" == "--help" ]]; then

	## Put your documentation here, a single line is best

	echo "[builtin] delete a script"

	## End help line

	exit
fi

## Put your commands that are within the DSS here

## End commands


# Import system path
export PATH=$OLD_SYSTEM_PATH

## Put system commands here
if [[ "$#" -eq 1 ]]; then
	/bin/rm -f $DSS_ROOT/bin/$1
else
	echo "Please give me an argument, a script to nuke"
fi

## End commands
